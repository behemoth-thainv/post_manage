steps:
  # Build the Docker image
  - name: 'gcr.io/cloud-builders/docker'
    args:
      - 'build'
      - '--platform=linux/amd64'
      - '-t'
      - 'asia-northeast1-docker.pkg.dev/glossy-attic-443507-u6/docker/frontend:$SHORT_SHA'
      - '-t'
      - 'asia-northeast1-docker.pkg.dev/glossy-attic-443507-u6/docker/frontend:latest'
      - '.'
    dir: 'frontend'

  # Push the Docker image with SHORT_SHA tag
  - name: 'gcr.io/cloud-builders/docker'
    args:
      - 'push'
      - 'asia-northeast1-docker.pkg.dev/glossy-attic-443507-u6/docker/frontend:$SHORT_SHA'
    dir: 'frontend'

  # Push the Docker image with latest tag
  - name: 'gcr.io/cloud-builders/docker'
    args:
      - 'push'
      - 'asia-northeast1-docker.pkg.dev/glossy-attic-443507-u6/docker/frontend:latest'
    dir: 'frontend'

  # Update Kubernetes deployment (optional)
  - name: 'gcr.io/cloud-builders/kubectl'
    args:
      - 'set'
      - 'image'
      - 'deployment/frontend'
      - 'frontend=asia-northeast1-docker.pkg.dev/glossy-attic-443507-u6/docker/frontend:$SHORT_SHA'
      - '-n'
      - 'demo'
    env:
      - 'CLOUDSDK_COMPUTE_REGION=us-west1'
      - 'CLOUDSDK_CONTAINER_CLUSTER=autopilot-cluster'

images:
  - 'asia-northeast1-docker.pkg.dev/glossy-attic-443507-u6/docker/frontend:$SHORT_SHA'
  - 'asia-northeast1-docker.pkg.dev/glossy-attic-443507-u6/docker/frontend:latest'

# Options:
# machineType: Uses n1-standard-1 by default (cheaper, within free tier)
# For faster builds, uncomment and use: 'E2_HIGHCPU_8' or 'N1_HIGHCPU_8' (costs more)
options:
  # machineType: 'E2_HIGHCPU_8'  # Uncomment for faster builds (~$0.0136/min)
  logging: CLOUD_LOGGING_ONLY

timeout: '1200s'
